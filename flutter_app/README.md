# BibleSsam Flutter 앱

- **플랫폼**: iOS, Android, Web
- Flutter SDK 설치 후 프로젝트 루트에서 실행:
  ```bash
  flutter create . --project-name bible_ssam
  ```
  → `android/`, `ios/`, `web/` 폴더가 생성됩니다.
- 실행: `flutter run` (기기/에뮬레이터 연결 시) 또는 `flutter run -d chrome` (웹)
